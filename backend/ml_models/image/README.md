The image model does **not** use this folder for class names.

YOLOv8 reads animal names from `best.pt` while it looks at the photo
(Tiger, Elephant, Leopard, and the rest baked into the weights).

Put `best.pt` in this folder. No labels.json is required.
