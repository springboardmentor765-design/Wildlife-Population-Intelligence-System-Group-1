Place your trained **voice / audio** model in this folder.

Recommended filename: `wildlife_voice.pt` or `best.pt`

Also accepted: `wildlife_audio.pt`, `model.keras`, `model.h5`, `yamnet.tflite`, `model.onnx`

Edit `labels.json` so class indexes match Species.common_name in the database.
